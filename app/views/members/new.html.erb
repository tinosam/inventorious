<body class="hold-transition skin-blue sidebar-mini">
<%= render 'layouts/header' %>
<div class="content-wrapper">

  <!-- Content Header (Page header) -->
  <section class="content-header">
    <h1>
      Ajouter membre
    </h1>
    <ol class="breadcrumb">
      <li><a href="#"><i class="fa fa-home"></i>Acceuil</a></li>
      <li class="active">Ajouter membre</li>
    </ol>
  </section>

  <!-- Main content -->
  <section class="content">
    <div class="row">
      <!-- left column -->
      <div class="col-md-6">
        <!-- general form elements -->
        <div class="box box-header">
          <div class="box-header with-border">
            <h3 class="box-title">Information basic</h3>
          </div>
          <!-- /.box-header -->
          <!-- form start -->
          <%= form_for(@member) do |f| %>
            <% if @member.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@member.errors.count, "error") %>Interdit à ce membre d'être sauvé:</h2>

                <ul>
                <% @member.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
                </ul>
              </div>
            <% end %>

            <div class="box-body">
             <div class="form-group">
                <label>Nom Complet</label>
                <%= f.text_field :name, autocomplete: "off", placeholder: "Entrer nom complet", class: "form-control", required: true %>
              </div>
             <div class="form-group">
                <label>Address E-mail </label>
                <%= f.text_field :email, autocomplete: "off", placeholder: "Entrer adresse email", class: "form-control", required: true %>
              </div>
              <div class="form-group">
                <label>Mobile </label>
                <%= f.text_field :phone, autocomplete: "off", placeholder: "Entrer mobile", class: "form-control" %>
              </div>
            <!-- /.box-body -->
            <div class="box-footer">
              <%= f.submit "Envoyer", class: "btn btn-primary"%>
            </div>
            <% end %>
        </div>
        <!-- /.box -->
      </div>
      <!--/.col (right) -->
    </div>
    </div>
    <!-- /.row -->
  </section>
  <!-- /.content -->

</div>
<%= render 'layouts/footer' %>
</body>
